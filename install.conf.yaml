- defaults:
  link:
    create: true
    relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.oh-my-zsh: .oh-my-zsh/
    ~/.config/starship.toml: starship.toml
    ~/.config/nvim: nvim/
    ~/.dotfiles: ''
  
