- defaults:
  link:
    create: true
    relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc
    ~/.oh-my-zsh: ohmyzsh/
    ~/.config/starship.toml: starship.toml
    ~/.config/nvim: nvim/
    ~/.dotfiles: ''
  
